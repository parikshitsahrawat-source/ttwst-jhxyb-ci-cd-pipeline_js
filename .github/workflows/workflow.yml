name: CI workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

job:
    runs-on: ubuntu-latest
    containers: node:20-alpine
      python:
        image: python:3.11